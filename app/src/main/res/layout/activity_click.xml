<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_click"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.github.lfuelling.lolclick.ClickActivity"
    android:background="@mipmap/background"
    >

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Display3"
        android:text="@string/start_clicking"
        android:textAlignment="center"
        android:id="@+id/scoreView"
        android:textColor="@color/white"
        android:shadowColor="@color/black"/>

    <ImageButton android:layout_height="@dimen/btn_size"
        android:layout_width="@dimen/btn_size"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:id="@+id/click_btn"
        android:background="@mipmap/ic_alone"/>

    <ImageView
        android:layout_width="@dimen/multi_size"
        android:layout_height="@dimen/multi_size"
        android:id="@+id/multiplierView"
        android:src="@mipmap/multi2x"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:visibility="gone"/>

</RelativeLayout>
